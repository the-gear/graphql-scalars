/**
 * @type {import('./config/types').PrettierConfig}
 */
const config = {
  singleQuote: true,
  arrowParens: 'always',
  semi: true,
  bracketSpacing: true,
  trailingComma: 'all',
  printWidth: 80,
};

module.exports = config;
